[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback (harmless with hash routing; future-proofs history routing).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
